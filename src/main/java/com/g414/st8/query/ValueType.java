package com.g414.st8.query;

/**
 * These are the value types we support Right Now(TM). Someday these may grow to
 * support amazing new value types.
 */
public enum ValueType {
    INTEGER, DECIMAL, STRING, BOOLEAN, NULL;
}
